"""Inline code-suggestion publishing and tracking (phase 1).

This package holds the additive "inline suggestion dual-publishing" feature:
publishing selected ``/improve`` code suggestions as GitLab-native inline
suggestions (Apply / Resolve) on the exact diff lines, and persisting a light
record of each published thread for later status tracking.
"""
